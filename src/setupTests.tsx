import { getRandomArbitrary } from "./App"
// jest-dom adds custom jest matchers for asserting on DOM nodes.
// allows you to do things like:
// expect(element).toHaveTextContent(/react/i)
// learn more: https://github.com/testing-library/jest-dom
import "@testing-library/jest-dom"
// test.todo("makebombs")
// test('getRandomArbitrary', () => {
//   expect(getRandomArbitrary(5, 10))
// })
// gérer si bombs > taille grille

// ne pas pouvoir mettre de drapeau avant le début

// ne pas pouvoir cliquer sur une bombe au premier click
